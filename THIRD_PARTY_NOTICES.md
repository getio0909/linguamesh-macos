# Third-Party Notices

The repository foundation contains no bundled third-party runtime code, binary artifact, image, font, provider logo, or generated SDK.

GitHub Actions uses `actions/checkout` under its published upstream license. It is CI infrastructure and is not distributed as part of the application.

Before adding a dependency or distributable asset, record its name, version, source, license, purpose, modification status, and distribution obligations here. Review transitive dependencies and avoid AGPL, SSPL, non-commercial, source-available, or otherwise incompatible terms. Apple SDK components must not be copied into the repository.

Notices for the pinned LinguaMesh Core XCFramework and localization bundle must be incorporated before any application artifact is distributed.

